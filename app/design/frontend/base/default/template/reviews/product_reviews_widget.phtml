<?php
$enabled  = Mage::getStoreConfig('reviewscouk_reviews_settings/widget/product_widget_enabled', Mage::app()->getStore());
if ($enabled){
    echo Mage::helper('reviewshelper')->getProductWidget(); 
}
